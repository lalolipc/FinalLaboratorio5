package com.utn.gocella.service;

import com.utn.gocella.model.Printer;
import com.utn.gocella.repository.PrinterRepository;
import org.junit.Before;
import org.junit.Test;
import org.mockito.Mock;
import org.springframework.http.ResponseEntity;

import java.util.ArrayList;
import java.util.List;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.*;
import static org.mockito.MockitoAnnotations.initMocks;

public class PrinterServiceTest {
/*
    PrinterService personService;
    @Mock
    PrinterRepository personRepository;

    @Before
    public void setUp() {
        initMocks(this);
        personService = new PrinterService(personRepository);
    }

    @Test
    public void testgetPersonByIdOk() {
        Printer one = Printer.builder().id(10).brand("n").model("nn").operativesistem("xp").build();


        when(personRepository.findById(1)).thenReturn(java.util.Optional.ofNullable(one));
        List<Printer> lP = personService.getAllPrinters();

        assertEquals(lP.get(1).getBrand(), one.getBrand());

    }
*/

}
